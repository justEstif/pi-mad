# Coverage Gaps

Areas of {{PRODUCT_NAME}} where **no standard exists yet**. This file makes missing guidance explicit instead of letting agents improvise silently.

> SCAFFOLD — starts as the only honest content in this skill: a list of what the team has not decided. Never fill a gap by inventing a standard here; gaps close only through the maintenance loop (evidence → proposal → human acceptance).

Load when: a routed reference has no accepted guidance for the current problem, or the same judgment call keeps coming up in review.

Canonical owner: TODO(owner/team)

## What belongs here

<!-- Guidance: one row per uncovered area. When the team accepts a standard, move the decision to its narrowest destination (the routed reference, an exemplar, a lint rule) and delete the row. -->

| Area / decision agents keep making | Where it came up (evidence) | Candidate direction, if any | Status |
| ---------------------------------- | --------------------------- | --------------------------- | ------ |
| TODO                               | TODO                        | TODO                        | open   |

## Maintenance

- Add a row whenever an agent or reviewer hits a judgment call with no recorded standard.
- Review rows on a regular cadence; propose guideline updates from the evidence; a human accepts or rejects each.
- Closing a gap means writing an accepted record in the narrowest relevant reference — never silently adopting a convention because one PR did it that way.
